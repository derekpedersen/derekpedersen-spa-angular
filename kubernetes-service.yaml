apiVersion: v1
kind: Service
metadata:
  labels:
    app: celebrityskateboards-spa-angular-deployment
  name: celebrityskateboards-spa-angular-service
  namespace: default
spec:
  ports:
  - name: 4200-to-80-tcp
    port: 4200
    protocol: TCP
    targetPort: 80
  selector:
    app: celebrityskateboards-spa-angular-deployment
  type: ClusterIP